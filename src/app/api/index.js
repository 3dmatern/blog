import users from "./fake.api/user.api";
import articles from "./fake.api/article.api";
import comments from "./fake.api/comment.api";
import tags from "./fake.api/tag.api";
import articlesTags from "./fake.api/articleTag.api";

const API = {
    users,
    articles,
    comments,
    tags,
    articlesTags,
};

export default API;
